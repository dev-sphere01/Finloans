{
  "name": "finloans",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "fin": "concurrently -n \"API,UI\" -c \"blue.bold,magenta.bold\" \"npm run dev --prefix api\" \"npm run dev --prefix ui\""
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
